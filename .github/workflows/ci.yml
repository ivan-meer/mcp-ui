name: CI

on:
  push:
    branches: [ main ] # Or your default branch
  pull_request:
    branches: [ main ] # Or your default branch

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8 # Specify a pnpm version, e.g., 8 or latest stable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Specify Node.js version
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint (Placeholder)
        run: echo "Linting step placeholder..." # Replace with actual lint command later

      - name: Build (Placeholder)
        # Attempt to build the examples package as a basic test of the toolchain
        run: pnpm --filter @auto-ui/examples build

      - name: Test (Placeholder)
        run: echo "Testing step placeholder..." # Replace with actual test command later
