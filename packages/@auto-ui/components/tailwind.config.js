/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,ts,jsx,tsx}", // Scan all relevant files in src
  ],
  theme: {
    extend: {
      // Theme values from @auto-ui/themes can be integrated here later
    },
  },
  plugins: [],
};
